{
  "name": "csu-trees",
  "version": "2.0.0",
  "description": "trees on the campus of colorado state university",
  "main": "index.js",
  "scripts": {
    "start": "web-dev-server --node-resolve --open --watch",
    "build": "copyfiles \"**/*\" -e 'node_modules/**/*' -e 'dist/**/*' -e 'LICENSE' -e 'package.json' -e 'package-lock.json' -e 'README.md' dist"
  },
  "author": "bright rain solutions",
  "license": "MIT",
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "copyfiles": "^2.4.1"
  },
  "dependencies": {
    "browser-sync": "^2.29.3"
  }
}
