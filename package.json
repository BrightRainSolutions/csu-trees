{
  "name": "csu-trees",
  "version": "0.0.1",
  "description": "trees on the campus of colorado state university",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --watch --browser \"chrome\"",
    "build": "copyfiles \"**/*\" -e 'node_modules/**/*' -e 'dist/**/*' dist"
  },
  "author": "bright rain solutions",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.4.1"
  },
  "dependencies": {
    "browser-sync": "^2.29.3"
  }
}
