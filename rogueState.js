// Note that the view and other arc things will not work if we make them reactive data
// so setting a global object
// We don't want them to be reactive anyway
export const rogueState = {
    view: Object,
    allTheTreesLayer: Object,
};